import React, {FC} from 'react';
import './Header.css';

const Header: FC = (() => {
    return (
        <header>
            <div className='header-title'>Tech Shopping Mall</div>
        </header>
    );
});

export default Header;
